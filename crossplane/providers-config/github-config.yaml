apiVersion: github.crossplane.io/v1beta1
kind: ProviderConfig
metadata:
  name: provider-github-config
spec:
  credentials:
    source: Secret
    secretRef:
      namespace: crossplane-system
      name: github-creds
      key: credentials