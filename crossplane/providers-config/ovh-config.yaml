apiVersion: ovh.edixos.io/v1beta1
kind: ProviderConfig
metadata:
  name: provider-ovh-config
spec:
  credentials:
    secretRef:
      key: credentials
      name: ovh-creds
      namespace: crossplane-system
    source: Secret